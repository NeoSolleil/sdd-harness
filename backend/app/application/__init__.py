"""Application layer: use cases and repository interfaces (abstractions).

Depends only on the domain layer. Defines repository interfaces; concrete
implementations live in the infrastructure layer (dependency inversion).
"""
