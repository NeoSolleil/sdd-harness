---
name: discover
description: SDD段階0（発見／Discovery）。新機能を作る最初に呼ぶ。Example Mapping（スリーアミーゴス）で機能の価値・ルール・具体例・疑問を多観点で洗い出し discovery.md にまとめる。EARS/Gherkin を清書する前の発見フェーズ。
argument-hint: [機能のアイデア・要望]
disable-model-invocation: true
---

# discover — 発見（SDD 段階0・Example Mapping）

新機能の最初の一歩。**いきなり要件やシナリオを書かない。** まず多観点で会話し、価値・ルール・具体例・疑問を洗い出す。出力は `specs/<feature>/discovery.md` のみ。

これは BDD の **Discovery** 工程。ここで固めた結果を、次の `specify` が EARS＋Gherkin に清書（Formulation）する。

> **実行（進行役への指示）**: product-analyst・quality-analyst・solution-analyst を **Agent ツールで並行に spawn**（1メッセージで3呼び出し）し、3観点（スリーアミーゴス）の草案を集める。各アナリストは**観点をテキストで返すだけ**。**統合と discovery.md への記述は進行役が1箇所で行う**（同じファイルへ同時書き込みしない）。委譲プロンプトには要望の全文・feature フォルダ（決まっていれば）・期待する返答形式（🟡🔵🟢🔴）を明記する（サブエージェントはこの会話を引き継がない）。

## 最初に参照する

- `ubiquitous-language`（用語集）… 用語を揃える。**発見は新しいドメイン用語の主な発生源**なので、新語は用語集への追加を提案する。

## 価値の検査（最初に通す関門）

- **「誰の・何を・なぜ」を1文で言えること。** 言えないなら、まだ作る段階でない。問い直す。
- **最小の縦切り（MVP）に絞る。** 「無くても価値が出る」ものは入れない。残りは「対象外」か将来の別機能フォルダへ。

## feature フォルダの採番

機能が定まったら **`specs/NNNN-kebab-name/`**（4桁連番＋ケバブ）を決めて作成し、`discovery.md` をそこに置く。番号は既存 `specs/` の最大＋1。例: `specs/0001-<機能名>/`。**以降の specify / plan / tasks / implement はこの同じフォルダを使う**（再採番しない）。

## 進め方（Example Mapping）

1つの機能を4種類に分解する。

- 🟡 **Story（機能）**: 対象機能を一文で。「誰が・何を・なぜ」。
- 🔵 **Rule（ルール）**: 守るべき受け入れ基準。→ 後の **EARS 要件**になる。
- 🟢 **Example（具体例）**: 各ルールを具体的な状況・値で表した例（正常／異常／境界）。→ 後の **Gherkin シナリオ**になる。
- 🔴 **Question（疑問）**: 未確定・前提・抜け漏れ・矛盾。**人間が答えるまで未解決として残す**（勝手に決めない）。

### 進行プロトコル（重要）

1. AI が下のレンズで多観点に🟡🔵🟢🔴の**草案**を作る。
2. **🔴疑問をまとめて人間に提示**し、回答を得る（選択肢があるものは AskUserQuestion で）。**AI は答えを発明しない。**
3. 回答で地図を更新。回答から新たな🔵🟢🔴が出たら追加する。
4. **🔴が残る間は Ready にしない・specify に進まない。** Ready 基準を満たすまで 2〜3 を反復。

## 多観点レンズ（抜け漏れと価値の検査）

各ルール・例を次のレンズで点検し、抜けがあれば 🔵🟢🔴 を足す：

- **プロダクト／価値**: 誰の何の課題か。価値はあるか。MVP は何か。**スコープ外**は何か。
- **開発／実現性**: 技術制約・設計への影響・既存との整合。
- **QA／テスト**: 異常系・境界・失敗モード（「どう壊れうるか」）。
- （必要に応じ UX ／ セキュリティ・データ ／ パフォーマンス）

## 大きすぎ／小さすぎの調整

- 🔵が多すぎる、または無関係な複数ゴールが混在 → **大きすぎ**。**複数の機能フォルダに分割**し、各フォルダで discover を回す。
- 🔵が1件しか出ない → **小さすぎ**（それは機能ではなく要件）。上位の機能に束ねる。
- 目安は 🔵 が数件〜十数件。

## Ready（準備OK）基準 — 満たすまで specify に進まない

- 🔴 未解決の疑問が **ゼロ**（すべて回答済み、または明示的に「対象外」と決定）。
- 🟡 価値が1文で言え、スコープの「含む／含まない」が明確。
- 🔵 が数件〜十数件で、各 🔵 に最低1つの 🟢。
- 満たしたら discovery.md 先頭の `Status:` を **Approved**（人間承認）にする。

## discovery.md の構成

```markdown
# <機能名>（Discovery）

Status: Draft        # 人間が承認したら Approved に変える（specify は Approved のみ着手）

## Story / 価値
誰の・何を・なぜ（1文）。提供価値。

## スコープ
- 含む / 含まない

## Example Map
### Rule 1: <ルール>
- 例: <具体例 正常>
- 例: <具体例 境界>
### Rule 2: ...

## 解決済みの疑問
- Q: ... → A: ...（決定の根拠）

## 対象外と決めたこと
```

## 記入例（書式見本・差し替え可）

> 以下は Example Map の**書式を示す中立例**。自プロジェクトの機能へ差し替える。

```markdown
Status: Draft

## Story / 価値
ユーザーが問い合わせを送信し、担当者が内容を受け取れる。

## スコープ
- 含む: 入力の検証・問い合わせの保存・受理/エラーの応答
- 含まない: 担当者の返信(0002)、通知(0003)、認証(0004)

## Example Map
### Rule 1: 必須項目が揃った送信は受理される
- 例: 氏名とメールを入力して送信 → 受理、保存される
- 例(境界): メールが最大長ちょうど → 受理
### Rule 2: 必須項目が空の送信は拒否される
- 例: メール未入力で送信 → 拒否、エラー表示、保存されない

## 解決済みの疑問
- Q: メール形式の検証はどこまで → A: MVP は「@ を含む」まで（厳密検証は将来）
- Q: 同一内容の連続送信の扱い → A: 直近と同一内容なら1件として扱う

## 対象外と決めたこと
- 担当者への通知（0003 で扱う）
```

## 制約（ハーネス）

- discovery.md だけを作る。EARS・Gherkin・コードは書かない（それは specify 以降）。
- 🔴 疑問を勝手に決めない。人間に確認する。
- スコープ厳守。価値が薄い／不明なら MVP を問い直す。

## 完了の定義

Ready 基準を満たし、`Status: Approved`（人間承認）になった discovery.md。→ 次は `specify`。
